{
    "layer": "top",
    "modules-left": ["sway/workspaces", "sway/mode"],
    "modules-center": ["sway/window"],
    "modules-right": ["battery", "pulseaudio", "disk", "memory", "temperature", "cpu", "clock"],
    "sway/window": {
        "max-length": 50
    },
    "battery": {
        "format": "{capacity}% {icon}",
        "format-icons": ["", "", "", "", ""],
        "interval": 5
    },
    "disk": {
        "path": "/",
        "interval": 30,
        "format": "{percentage_used}% "
    },

    "pulseaudio": {
        "format": "{volume}% {icon}",
        "format-muted": "",
        "on-click": "pavucontrol",
        "format-icons": {
            "default": ["", ""]
        }
    },

    "temperature": {
        "format": "{temperatureC}°C "
    },

    "memory": {
        "interval": 10,
        "format": "{used:0.1f}G/{total:0.1f}G "
    },

    "cpu": {
        "interval": 10,
        "format": "{load} {usage}% ",
        "max-length": 15
    },
    "clock": {
        "interval": 1,
        "format": "{:%H:%M:%S}  ",
        "format-alt": "{:%a, %d. %b  %H:%M:%S}  "
    }
}
